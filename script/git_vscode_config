#!/bin/sh

git config --global --add safe.directory "%(prefix)///192.168.1.7/share/mimao-littlemouse.github.io"
# 复制到 vscode的终端中 并运行(注意：ip地址需要更改为虚拟机中的ip地址)，
# 以确保 使用虚拟机运行项目时，在windows上使用vscode编辑项目时，git可以正常工作