#!/bin/sh

cat << EOF

jekyll new PATH 在指定的路径上创建一个新的jekyll站点，该站点具有默认的基于gem的主题。将根据需要创建目录。

jekyll new PATH--blank 在指定的路径上创建一个新的空白jekyll站点脚手架。

jekyll build或jekyll b 执行一次性构建您的网站/_站点（默认情况下）。

jekyll serve或jekyll s 在源文件更改时构建您的站点，并在本地提供服务。

jekyll clean 删除所有生成的文件：目标文件夹、元数据文件、Sass和jekyll缓存。

jekyll help 显示帮助，可选择用于给定的子命令
            例如jekyll help build

jekyll new-theme 一个新的杰基尔主题脚手架。

jekyll doctor 输出任何弃用或配置问题。

EOF